language: python
python:
  - "3.5"
  - "3.6"
  - "3.7"
  - "3.8"
  - "3.9"
before_install:
  - sudo apt-get update
  - sudo apt-get -y install libsystemd-dev
install: pip install -r .travis_require
script: make test

# Local Variables:
# mode: yaml
# End:
